# =============================================================================
# Pose Transfer Configuration v17 (Ghost Filter v3.1)
# =============================================================================

# [1] 모델 설정
model:
  backend: "onnxruntime"
  device: "cuda"
  mode: "performance"
  to_openpose: false

# [2] 다중 인물 필터링
person_filter:
  enabled: true
  area_weight: 0.6
  center_weight: 0.4
  min_keypoints: 5
  confidence_threshold: 0.3

# [3] 손 정밀화 (작은 손 업스케일링)
hand_refinement:
  enabled: true
  min_hand_size: 48
  max_scale_factor: 4.0

# =============================================================================
# [4] 포즈 전이 설정
# =============================================================================
transfer:
  normalization_base: "shoulder_width"
  confidence_threshold: 0.3
  visibility_margin: 0.5

# =============================================================================
# [5] Ghost Filter v3.1 (수동 조절 가능)
# =============================================================================
# 
# 핵심 정책:
# 1. 프레임 이탈 체크: 이미지 경계 밖 키포인트는 score=0
# 2. 경계값 감지: (0, y), (x, 0), (w-1, y), (x, h-1) 등 경계 좌표 무효
# 3. 클러스터링 체크: 손 키포인트가 한 점에 뭉쳐있으면 무효
# 4. Src ∩ Ref: 양쪽 모두 존재하는 키포인트만 trans에 생성
#
# =============================================================================
ghost_filter:
  # ───────────────────────────────────────────────────────────────
  # 기본 설정
  # ───────────────────────────────────────────────────────────────
  enabled: true                    # Ghost Filter 전체 on/off
  confidence_threshold: 0.1        # 키포인트 존재 판단 임계값
  
  # ───────────────────────────────────────────────────────────────
  # [A] 프레임 이탈 체크
  # ───────────────────────────────────────────────────────────────
  check_bounds: true               # 프레임 이탈 체크 on/off
  bounds_margin: 0.05               # 프레임 경계 마진 (0.0 = 엄격, 0.05 = 5% 여유)
  
  # ───────────────────────────────────────────────────────────────
  # [B] 경계값 감지 (DWPose 감지 실패 패턴)
  # ───────────────────────────────────────────────────────────────
  # DWPose가 감지 실패 시 좌표를 (0, y), (x, 0), (w-1, y), (x, h-1)에 찍음
  # 이 패턴을 감지하여 제거
  #
  check_boundary_values: true      # 경계값 감지 on/off
  boundary_tolerance: 3.0          # 경계에서 몇 px 이내를 무효로 판단
                                   # 예: 3.0 → x가 0~3 또는 w-4~w-1 범위면 무효
                                   # 값을 높이면 더 많이 제거, 낮추면 덜 제거
  
  # ───────────────────────────────────────────────────────────────
  # [C] 예측 가능 키포인트 허용
  # ───────────────────────────────────────────────────────────────
  # 프레임 밖이지만 인접 키포인트가 유효하면 허용하는 범위
  #
  allow_predictable: true          # 예측 가능 허용 on/off
  predictable_margin: 0.15         # 프레임 밖 허용 범위 (0.15 = 15%)
                                   # 예: 이미지 폭 1000px → 프레임 밖 150px까지 허용
  min_confidence_for_prediction: 0.3  # 인접 키포인트 최소 신뢰도
  
  # ───────────────────────────────────────────────────────────────
  # [D] 클러스터링 체크 (손/하반신 키포인트)
  # ───────────────────────────────────────────────────────────────
  # DWPose가 손을 감지 못하면 모든 키포인트를 한 점에 찍는 경향이 있음
  # 이 패턴을 감지하여 전체 무효
  #
  check_clustering: true           # 클러스터링 체크 on/off
  min_cluster_spread: 20.0         # 최소 퍼짐 정도 (px)
                                   # 손 키포인트들의 표준편차가 이보다 작으면 무효
                                   # 예: 20.0 → 손 키포인트가 20px 이내에 뭉쳐있으면 무효
                                   # 값을 높이면 더 엄격, 낮추면 더 관대
  cluster_distance_threshold: 5.0  # (현재 미사용, 예비)
  
  # ───────────────────────────────────────────────────────────────
  # [E] 하단 영역 클러스터링 체크 (Ghost Leg 감지)
  # ───────────────────────────────────────────────────────────────
  # DWPose가 하반신을 감지 못하면 이미지 하단에 모든 키포인트를 찍음
  # hip, knee, ankle이 하단 15% 영역에 뭉쳐있으면 무효
  #
  # bottom_ratio: 0.15 → 이미지 하단 15% 영역 체크
  # bottom_y_spread: 50 → y 방향 50px 이내에 뭉쳐있으면 Ghost

# =============================================================================
# [6] Ghost Filter 튜닝 가이드
# =============================================================================
#
# 📌 문제: 손/발 키포인트가 여전히 이상한 위치에 있음
#    → boundary_tolerance 값을 높임 (예: 3.0 → 10.0)
#    → min_cluster_spread 값을 높임 (예: 20.0 → 50.0)
#
# 📌 문제: 정상적인 키포인트도 제거됨
#    → boundary_tolerance 값을 낮춤 (예: 3.0 → 1.0)
#    → check_boundary_values: false 로 끔
#
# 📌 문제: 프레임 경계 근처 키포인트가 사라짐
#    → bounds_margin 값을 높임 (예: 0.0 → 0.05)
#    → predictable_margin 값을 높임 (예: 0.15 → 0.25)
#
# 📌 문제: Ghost Leg이 여전히 나타남
#    → check_bounds: true 확인
#    → confidence_threshold 값을 높임 (예: 0.1 → 0.3)
#
# =============================================================================

# [7] 폴백 전략 (누락된 부위 복구)
fallback:
  symmetric_mirror: true
  hierarchical_estimation: true
  fallback_confidence_decay: 0.5

# [8] 렌더링 스타일
rendering:
  line_thickness: 4
  face_line_thickness: 2
  hand_line_thickness: 4
  point_radius: 2
  background_color: [0, 0, 0]
  kpt_threshold: 0.1

# [9] 출력 설정
output:
  save_json: true
  save_skeleton_image: true
  save_debug_image: true
  auto_crop_enabled: false
  canvas_padding_ratio: 0
  crop_padding_px: 0
  head_padding_ratio: 0.5

# [10] 시스템 설정
system:
  retain_inputs: true
  enable_archiving: false

# [11] 디버그 설정 (BBox 확인용)
debug:
  bbox_visualization: true
  visualize_keypoint_bbox: true
  visualize_yolo_bbox: true
  visualize_hybrid_bbox: true

# [12] 정렬 설정
alignment:
  full_body_min_valid_lower: 4
  yolo_verification_enabled: true
  yolo_person_conf: 0.5
  yolo_face_conf: 0.3
  face_scale_enabled: true

# [13] Bbox 여백
bbox:
  person_margin: 0.0
  face_margin: 0.0

# [14] 얼굴 렌더링 (부위별 제어)
face_rendering:
  enabled: true
  parts:
    jawline:
      enabled: false
      color: [255, 255, 255]
    left_eyebrow:
      enabled: true
      color: [255, 255, 255]
    right_eyebrow:
      enabled: true
      color: [255, 255, 255]
    nose:
      enabled: true
      color: [255, 255, 255]
    left_eye:
      enabled: true
      color: [255, 255, 255]
    right_eye:
      enabled: true
      color: [255, 255, 255]
    mouth_outer:
      enabled: true
      color: [255, 255, 255]
    mouth_inner:
      enabled: true
      color: [255, 255, 255]